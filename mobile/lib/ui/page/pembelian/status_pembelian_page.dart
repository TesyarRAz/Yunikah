import 'package:flutter/material.dart';

class StatusPembelianPage extends StatelessWidget {
  int initialIndex;
  StatusPembelianPage({required this.initialIndex});
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}